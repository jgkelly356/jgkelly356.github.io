<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Joseph Kelly | Portfolio</title>
  <meta http-equiv="refresh" content="3; url=https://v0-jgkelly356.vercel.app/" />
  <style>
    :root {
      --bg: #070b12;
      --panel: rgba(255,255,255,0.04);
      --panel-strong: rgba(255,255,255,0.07);
      --border: rgba(255,255,255,0.1);
      --text: rgba(255,255,255,0.94);
      --muted: rgba(255,255,255,0.68);
      --accent: #7dd3fc;
      --accent-2: #93c5fd;
      --shadow: 0 20px 60px rgba(0,0,0,0.35);
    }

    * {
      box-sizing: border-box;
    }

    html, body {
      margin: 0;
      padding: 0;
      min-height: 100%;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background:
        radial-gradient(circle at top left, rgba(125, 211, 252, 0.08), transparent 30%),
        radial-gradient(circle at bottom right, rgba(147, 197, 253, 0.08), transparent 28%),
        var(--bg);
      color: var(--text);
    }

    body {
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 32px 20px;
    }

    .card {
      width: 100%;
      max-width: 760px;
      background: linear-gradient(180deg, var(--panel-strong), var(--panel));
      border: 1px solid var(--border);
      border-radius: 24px;
      box-shadow: var(--shadow);
      padding: 40px 32px;
      backdrop-filter: blur(12px);
    }

    .eyebrow {
      display: inline-block;
      font-size: 12px;
      letter-spacing: 0.14em;
      text-transform: uppercase;
      color: var(--accent);
      margin-bottom: 16px;
      font-weight: 600;
    }

    h1 {
      margin: 0 0 12px 0;
      font-size: clamp(2rem, 5vw, 3.4rem);
      line-height: 1.02;
      letter-spacing: -0.04em;
    }

    p {
      margin: 0;
      color: var(--muted);
      font-size: 1.05rem;
      line-height: 1.7;
      max-width: 60ch;
    }

    .actions {
      display: flex;
      flex-wrap: wrap;
      gap: 14px;
      margin-top: 28px;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      border-radius: 14px;
      padding: 13px 18px;
      font-weight: 600;
      font-size: 0.98rem;
      transition: transform 0.18s ease, opacity 0.18s ease, border-color 0.18s ease, background 0.18s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--accent), var(--accent-2));
      color: #06111c;
    }

    .btn-secondary {
      background: rgba(255,255,255,0.03);
      color: var(--text);
      border: 1px solid var(--border);
    }

    .meta {
      margin-top: 26px;
      padding-top: 20px;
      border-top: 1px solid var(--border);
      color: var(--muted);
      font-size: 0.95rem;
      line-height: 1.6;
    }

    .meta a {
      color: var(--accent);
      text-decoration: none;
    }

    .meta a:hover {
      text-decoration: underline;
    }

    .small {
      margin-top: 10px;
      font-size: 0.88rem;
      color: rgba(255,255,255,0.52);
    }

    @media (max-width: 640px) {
      .card {
        padding: 28px 22px;
        border-radius: 20px;
      }

      p {
        font-size: 1rem;
      }

      .actions {
        flex-direction: column;
      }

      .btn {
        width: 100%;
      }
    }
  </style>
</head>
<body>
  <main class="card">
    <div class="eyebrow">Joseph Kelly</div>
    <h1>My portfolio has moved.</h1>
    <p>
      I recently updated my engineering portfolio and moved it to a new site. You should be redirected automatically in a few seconds.
    </p>

    <div class="actions">
      <a class="btn btn-primary" href="https://v0-jgkelly356.vercel.app/">Open Updated Portfolio</a>
      <a class="btn btn-secondary" href="/Joseph-Kelly-Resume.pdf">View Resume</a>
    </div>

    <div class="meta">
      This page stays here so existing links still work while pointing visitors to the updated version.
      <div class="small">
        If the redirect does not happen, use the button above.
      </div>
    </div>
  </main>
</body>
</html>